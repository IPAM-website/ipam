import { component$ } from '@builder.io/qwik';

export interface ClientListProps {

}

export const ClientList = component$<ClientListProps>((props) => {
  return (
    <div>
      ClientList component works!
    </div>
  );
});
